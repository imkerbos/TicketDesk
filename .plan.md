# 统一流程视图：工作流与状态流转合二为一

## 问题

当前有两套不同的流程体验：
1. **有工作流**：显示工作流卡片（审批/完成按钮）+ "查看完整流程"按钮打开节点流程图
2. **无工作流**：显示"状态流转"下拉菜单，没有流程图

用户希望：**不管有没有工作流，都用同一套流程图 UI 来展示和操作**。

## 设计方案

### 核心思路

将"查看完整流程"改为**始终可见**。无工作流时，前端生成一个**虚拟状态机流程图**，节点可点击进行状态流转。有工作流时保持现有行为不变。

### 改动范围

**仅修改前端** `web/src/views/issue/IssueDetail.vue`，后端无需改动。

---

## 具体改动

### 1. "查看完整流程"按钮始终可见

```
之前: <el-button v-if="workflowInstance" ...>查看完整流程</el-button>
之后: <el-button v-if="issue" ...>查看完整流程</el-button>
```

### 2. 流程图对话框支持两种模式

**`showWorkflowDiagram()` 方法改造：**

- 有 `workflowInstance` → 走现有逻辑（API 获取 nodes/edges）
- 无 `workflowInstance` → 生成虚拟状态机节点

**虚拟状态机节点（主线性流程）：**
```
[待处理] ──→ [进行中] ──→ [已完成] ──→ [已终止]
  open       in_progress    resolved      closed
```

`重新打开(reopened)` 作为回退节点，显示在第二行：
```
                        ↗ [重新打开] ↘
[待处理] ──→ [进行中] ──→ [已完成] ──→ [已终止]
```

实际实现简化为线性排列（与工作流模式一致）：
```
[待处理] → [进行中] → [已完成] → [重新打开] → [已终止]
```

### 3. 状态机节点的状态判断

根据 `issue.status` 判断每个节点的状态：

| issue.status | 待处理 | 进行中 | 已完成 | 重新打开 | 已终止 |
|---|---|---|---|---|---|
| open | **current** | pending | pending | pending | pending |
| in_progress | visited | **current** | pending | pending | pending |
| resolved | visited | visited | **current** | pending | pending |
| reopened | visited | visited | visited | **current** | pending |
| closed | visited | visited | visited | - | **current** |

### 4. 可点击节点交互（仅状态机模式）

在状态机模式下，根据 `getAvailableTransitions(issue.status)` 判断哪些节点可点击：
- 可流转的目标节点：添加 `clickable` CSS 类（cursor: pointer + 虚线高亮边框）
- 点击后弹出确认框 → 执行 `transitionIssue` → 刷新页面
- 不可流转的节点：不可点击

### 5. 保留状态流转下拉菜单

下拉菜单作为快捷操作保留，流程图提供全局视图 + 可视化操作。

---

## 实现细节

### 新增数据结构

```typescript
// 状态机虚拟节点（扩展 WorkflowNode，增加 status 字段）
interface StatusFlowNode extends Partial<WorkflowNode> {
  id: number
  name: string
  node_type: string
  status?: string  // 对应的 issue status
}

// 是否为状态机模式
const isStatusFlowMode = computed(() => !workflowInstance.value)
```

### `showWorkflowDiagram()` 改造

```typescript
const showWorkflowDiagram = async () => {
  diagramVisible.value = true

  if (workflowInstance.value) {
    // 工作流模式：从 API 获取（现有逻辑不变）
    diagramLoading.value = true
    try { ... } finally { diagramLoading.value = false }
  } else {
    // 状态机模式：前端生成虚拟节点
    diagramNodes.value = generateStatusFlowNodes()
    diagramEdges.value = []  // 状态机不需要 edges
  }
}
```

### `generateStatusFlowNodes()` 方法

```typescript
const statusFlowDefinition = [
  { id: 1, name: '待处理', node_type: 'start', status: 'open' },
  { id: 2, name: '进行中', node_type: 'work', status: 'in_progress' },
  { id: 3, name: '已完成', node_type: 'work', status: 'resolved' },
  { id: 4, name: '已终止', node_type: 'end', status: 'closed' },
  { id: 5, name: '重新打开', node_type: 'work', status: 'reopened' },
]
```

### `getNodeDiagramClass()` 改造

增加状态机模式的判断逻辑：

```typescript
const getNodeDiagramClass = (node: any) => {
  if (isStatusFlowMode.value) {
    // 状态机模式
    const currentStatus = issue.value?.status
    const nodeStatus = node.status
    const isCurrent = nodeStatus === currentStatus
    const isVisited = getStatusOrder(nodeStatus) < getStatusOrder(currentStatus)
    const isClickable = availableTransitionStatuses.value.includes(nodeStatus)
    return { current: isCurrent, visited: isVisited, pending: !isCurrent && !isVisited, clickable: isClickable }
  }
  // 工作流模式（现有逻辑不变）
  ...
}
```

### 节点点击处理

```typescript
const handleDiagramNodeClick = async (node: any) => {
  if (!isStatusFlowMode.value || !node.status) return
  if (!availableTransitionStatuses.value.includes(node.status)) return

  const label = statusFlowDefinition.find(s => s.status === node.status)?.name
  await ElMessageBox.confirm(`确定要将状态流转为「${label}」吗？`, '状态流转')
  await transitionIssue(issue.value!.issue_key, node.status)
  ElMessage.success('状态更新成功')
  diagramVisible.value = false
  loadIssue()
}
```

### CSS 新增 `.clickable` 样式

```scss
.diagram-node.clickable {
  cursor: pointer;
  border-style: dashed;
  border-color: #409eff;

  &:hover {
    background: #ecf5ff;
    border-color: #409eff;
    border-style: solid;
    box-shadow: 0 0 0 3px rgba(64, 158, 255, 0.15);
    transform: translateY(-2px);
  }
}
```

---

## 验证

1. **有工作流的工单**：点击"查看完整流程"→ 显示工作流节点图（与现有行为一致）
2. **无工作流的工单**：点击"查看完整流程"→ 显示状态机流程图，当前状态蓝色高亮
3. **状态机模式**：可流转的目标节点显示虚线边框，鼠标悬停高亮，点击后执行状态流转
4. **状态流转下拉菜单**仍然可用
5. 流程图中已经过的状态显示为绿色
