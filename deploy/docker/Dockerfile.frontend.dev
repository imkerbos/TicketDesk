# 前端开发镜像（支持 Vite 热更新）
FROM node:20

WORKDIR /app

# 设置时区
ENV TZ=Asia/Shanghai

# 设置 Docker 环境变量
ENV DOCKER_ENV=true

# 复制 package.json 并安装依赖
COPY package*.json ./
RUN npm install

# 暴露端口
EXPOSE 3100

# 启动开发服务器
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
